# /opt/deke/.env на сервере — читается docker-compose.prod.yml

# Образы (перезаписываются CI, но нужны для ручного запуска)
BACKEND_IMAGE=ghcr.io/OWNER/REPO/backend:latest
FRONTEND_IMAGE=ghcr.io/OWNER/REPO/frontend:latest

# Postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_STRONG
POSTGRES_DB=deke_db
